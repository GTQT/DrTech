package com.drppp.drtech.drone.program.model;

/** Editor-neutral property types shared by node declarations, compiler validation and future inspector controls. */
public enum DroneNodePropertyType {
    INTEGER,
    NUMBER,
    BOOLEAN,
    STRING,
    ENUM,
    ITEM_SELECTOR,
    BLOCK_SELECTOR,
    FLUID_SELECTOR,
    ENTITY_SELECTOR,
    DIRECTION,
    COORDINATE,
    AREA,
    PROGRAM_REFERENCE,
    DOCK_REFERENCE
}
